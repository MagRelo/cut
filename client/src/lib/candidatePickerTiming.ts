/** Hold the Saved! overlay before the picker starts closing. */
export const PICKER_SAVED_FLASH_MS = 450;
/** Let the dialog leave transition finish before clearing busy state. */
export const PICKER_LEAVE_MS = 200;
/** Minimum Saving… beat when there is no network round-trip (guest picker). */
export const PICKER_LOCAL_SAVE_MS = 250;
