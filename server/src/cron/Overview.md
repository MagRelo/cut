# Cron overview

See **[README.md](./README.md)** and **[`spec/server/cron.md`](../../../spec/server/cron.md)** for the v4 cron design.

Single pipeline (`mainPipeline`), every 5 minutes, when `ENABLE_CRON=true`.
